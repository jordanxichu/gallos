// Fix for isar_flutter_libs namespace issue with AGP 8+
allprojects {
    afterEvaluate {
        if (project.hasProperty('android') && project.name == 'isar_flutter_libs') {
            project.android {
                namespace 'dev.isar.isar_flutter_libs'
            }
        }
    }
}
